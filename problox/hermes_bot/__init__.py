"""Bot Discord "Hermès" : agent conversationnel (DeepSeek) avec outils
(recherche web, exécution de code sandboxée, sous-agents, génération de jeux
ProbloxDev) branché sur un serveur Discord.

Paquet optionnel : ne s'importe/installe qu'avec l'extra `hermes`
(`pip install -e ".[hermes]"`) pour ne pas alourdir l'install de base de
problox pour les gens qui n'utilisent que la CLI/le site.
"""
